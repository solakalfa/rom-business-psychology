---
import Layout from "../layouts/Layout.astro";
import TeamCard from "../components/TeamCard.astro";
import Footer from "../components/Footer.astro";
import { clients } from "../data/clients.js";

// Create rows of 4 logos each
const logosPerRow = 4;
const rows = [];
for (let i = 0; i < clients.length; i += logosPerRow) {
  rows.push(clients.slice(i, i + logosPerRow));
}

---

<Layout title="About Us - ROM Business Psychology">





  <!-- HERO SECTION 
<section class="relative min-h-[50vh] flex items-center pt-20 overflow-hidden">
  <img 
    src="https://images.unsplash.com/photo-1522071820081-009f0129c71c?w=1920&h=800&fit=crop" 
    alt="Our team"
    class="absolute inset-0 w-full h-full object-cover scale-110"
  />
  
  <div class="absolute inset-0 bg-black/70"></div>
  
  <div class="relative z-10 w-full">
    <div class="container mx-auto px-6 max-w-4xl py-20">
      <p class="text-cyan-400 text-sm uppercase tracking-widest mb-4 font-semibold">
        about us
      </p>
      <h1 class="text-white">
        We come from within
      </h1>
    </div>
  </div>
</section>-->

<!-- BUSINESS PSYCHOLOGY IS IN OUR VEINS -->
<!-- BREADCRUMB -->
<section class="bg-gray-50 py-4">
  <div class="container mx-auto px-6 max-w-4xl">
    <nav class="flex items-center text-sm text-gray-600">
      <a href="/" class="hover:text-cyan-600">Home</a>
      <svg class="w-4 h-4 mx-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
      </svg>
      <span class="text-gray-900">About</span>
    </nav>
  </div>
</section>

<!-- MISSION SECTION -->
<section class="bg-gray-50 py-16">
  

  <div class="container mx-auto px-6 max-w-4xl">
    <p class="text-cyan-600 font-semibold text-sm uppercase tracking-wider mb-4">
      Our Mission
    </p>
    <h1 class="text-gray-900 mb-6">
      Business Psychology is in Our Veins
    </h1>
    <p class="text-xl text-gray-600 leading-relaxed">
Our mission is to help assemble top flight, healthy organizational environments.

Together with the company's leadership, we implement simple and concrete managerial solutions that enhance effectiveness and mitigate current and future challenges.</div>
</section>

<!-- WE PROVIDE TRAINING & COUNSELING -->
<section class="bg-white py-16">
  <div class="container mx-auto px-6 max-w-4xl">
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
      
      <!-- Left Card -->
      <div class="bg-gradient-to-br from-cyan-500 to-blue-600 rounded-3xl p-12 text-white shadow-2xl">
        <h2 class="text-white mb-6 leading-tight">
          We provide training & counseling
        </h2>
        <p class="text-white/90 text-lg leading-relaxed">
          We've been around Israeli companies for many years.
        </p>
      </div>

      <!-- Right - Content -->
      <div class="space-y-4 text-lg text-gray-700 leading-relaxed">
        <p>Our knowledge combines a thorough understanding of human behavior along with comprehensive experience in the business ecosystem.</p>
        <p>Our services are focused on strengthening effective performance for individuals, teams, and organizations.</p>
      </div>

    </div>

  </div>
</section>
<!-- TEAM'S PASSION -->
<section class="bg-gray-50 py-20">
  <div class="container mx-auto px-6 max-w-4xl">
    
    <div class="mb-12">
      <p class="text-cyan-600 font-semibold text-sm uppercase tracking-wider mb-4">
        Our Team
      </p>
      
      <h2 class="text-gray-900 mb-6">
        Team's passion is to help people
      </h2>
      
      <div class="space-y-4 text-lg text-gray-700 leading-relaxed">
        <p>We are a group of practitioners from the fields of psychology and organizational development.</p>
        <p>We all hold significant managerial experience and advanced academic degrees.</p>
        <p>We are professionals so we make sure that we continue to learn and develop ourselves, receive supervision from field experts, and participate in continuous training.</p>
      </div>
    </div>

    <!-- TEAM GRID - Equal heights with auto-rows-fr -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 auto-rows-fr mt-16">

      <div class="h-full">
        <TeamCard 
          name="Eldad Rom (Ph.D.)"
          role="Founder & CEO"
          image="/team/dreldadrom.jpeg"
          memberId="eldad"
        />
      </div>
      
      <div class="h-full">
        <TeamCard 
          name="David Segal (Ph.D.)" 
          role="Clinical Psychologist" 
          image="/team/dr-david-segal.jpg" 
          memberId="david" 
        />
      </div>
      
      <div class="h-full">
        <TeamCard 
          name="Ganit Rainer Sassover (M.A.)" 
          role="Executive Coach" 
          image="/team/Ganit.jpg" 
          memberId="ganit" 
        />
      </div>
      
      <div class="h-full">
        <TeamCard 
          name="Netta Gefen (M.A.)" 
          role="Organizational Consultant" 
          image="/team/Netta-Gefen-Organizational-consultant.jpg" 
          memberId="netta" 
        />
      </div>
      
      <div class="h-full">
        <TeamCard 
          name="Meital Drezner-Maimon (M.A.)" 
          role="Organizational Consultant" 
          image="/team/meital.jpg" 
          memberId="meital" 
        />
      </div>
      
      <div class="h-full">
        <TeamCard 
          name="Anat Frishman (M.A.)" 
          role="Organizational Consultant" 
          image="/team/anat-1.jpg" 
          memberId="anat" 
        />
      </div>
      
      <div class="h-full">
        <TeamCard 
          name="Edna Arbel (M.A.)" 
          role="Executive Coach" 
          image="/team/עדנה-ארבל.jpg" 
          memberId="edna" 
        />
      </div>
      
      <div class="h-full">
        <TeamCard 
          name="Naama Zalzman (M.A.)" 
          role="Organizational Consultant" 
          image="/team/Naama.jpg" 
          memberId="naama"
        />
      </div>
      <!-- Quote Box -->
<div class="relative bg-gradient-to-br from-cyan-50 to-blue-50 border-2 border-cyan-200 p-8 rounded-2xl flex items-center justify-center text-center hover:border-cyan-400 transition-colors">
  <div class="absolute -top-3 -left-3 w-6 h-6 bg-cyan-600 rounded-full"></div>
  <blockquote class="text-base font-light text-gray-700">
    "When I build a team, I first look for people who love to win."
    <span class="text-sm mt-3 block text-cyan-600 font-semibold">— Ross Perot</span>
  </blockquote>
</div>

    </div>

  </div>
</section>


<!-- TRUSTED BY CLIENTS - ABOUT PAGE -->
<section class="bg-white py-20">
  <div class="container mx-auto px-6 max-w-4xl">
    
    <!-- Row 1: Text -->
    <div class="mb-12">
      <h2 class="text-gray-900 mb-6 leading-tight">
        Trusted by <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-600 to-blue-600">leading organizations</span>
      </h2>
      
      <p class="text-xl text-gray-600 leading-relaxed max-w-3xl">
        We've been privileged to work with over 167 organizations across Israel, from startups to Fortune 500 companies. Our clients trust us to strengthen their teams and develop their people.
      </p>
    </div>

    <!-- Row 2: Animated Logo Grid -->
    <div class="relative h-[300px] md:h-[400px] overflow-hidden rounded-2xl bg-gradient-to-br from-gray-50 to-white p-4 md:p-8 border border-gray-200">
      
      <div class="absolute inset-0 p-4 md:p-8">
        <div class="animate-scroll-up space-y-4 md:space-y-6">
          
          <!-- First Set -->
          {rows.map((row) => (
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-6">
              {row.map((client) => (
                <div class="bg-white p-3 md:p-4 rounded-lg shadow-sm hover:shadow-md transition-all hover:scale-105 flex items-center justify-center">
                  <img 
                    src={client.logo} 
                    alt={client.name}
                    class="h-10 md:h-12 w-full object-contain"
                  />
                </div>
              ))}
            </div>
          ))}
          
          <!-- Second Set - Duplicate for infinite scroll -->
          {rows.map((row) => (
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-6">
              {row.map((client) => (
                <div class="bg-white p-3 md:p-4 rounded-lg shadow-sm hover:shadow-md transition-all hover:scale-105 flex items-center justify-center">
                  <img 
                    src={client.logo} 
                    alt={client.name}
                    class="h-10 md:h-12 w-full object-contain"
                  />
                </div>
              ))}
            </div>
          ))}
          
        </div>
      </div>

      <!-- Top Gradient Overlay -->
      <div class="absolute top-0 left-0 right-0 h-20 md:h-24 bg-gradient-to-b from-gray-50 via-gray-50/80 to-transparent z-10 pointer-events-none"></div>
      
      <!-- Bottom Gradient Overlay -->
      <div class="absolute bottom-0 left-0 right-0 h-20 md:h-24 bg-gradient-to-t from-gray-50 via-gray-50/80 to-transparent z-10 pointer-events-none"></div>
      
    </div>

  </div>
</section>

<style>
  @keyframes scroll-up {
    0% { 
      transform: translateY(0); 
    }
    100% { 
      transform: translateY(-50%); 
    }
  }
  
  .animate-scroll-up {
    animation: scroll-up 120s linear infinite;
  }
  
  .animate-scroll-up:hover {
    animation-play-state: paused;
  }
</style>



<!-- VALUE SYSTEM -->
<section class="bg-gray-50 py-20">
  <div class="container mx-auto px-6 max-w-4xl">
    
    <div class="text-left mb-16">
  <p class="text-cyan-600 font-semibold text-sm uppercase tracking-wider mb-4">
    Our Values
  </p>

  <h2 class="text-gray-900 mb-6">
    Value system
  </h2>
  
  <p class="text-lg text-gray-600 max-w-2xl">
    Our core values guide everything we do and shape the way we work with our clients.
  </p>
</div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      
      <!-- Self-Discipline -->
      <div class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-shadow">
        <h3 class="text-2xl font-bold text-gray-900 mb-4 border-b-4 border-cyan-600 pb-3 inline-block">
          Self-discipline
        </h3>
        <p class="text-gray-600 text-lg leading-relaxed mt-6">
          We know that self-discipline is the common denominator of success.
        </p>
      </div>

      <!-- Curiosity -->
      <div class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-shadow">
        <h3 class="text-2xl font-bold text-gray-900 mb-4 border-b-4 border-blue-600 pb-3 inline-block">
          Curiosity
        </h3>
        <p class="text-gray-600 text-lg leading-relaxed mt-6">
          We know that we live in a knowledge era, so curiosity drives us.
        </p>
      </div>

      <!-- Results -->
      <div class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-shadow">
        <h3 class="text-2xl font-bold text-gray-900 mb-4 border-b-4 border-teal-600 pb-3 inline-block">
          Results
        </h3>
        <p class="text-gray-600 text-lg leading-relaxed mt-6">
          We are focused on results not intentions.
        </p>
      </div>

    </div>

  </div>
</section>

<!-- Popup Container -->
<div id="popup-container"></div>

<Footer />

</Layout>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const buttons = document.querySelectorAll('[data-member-id]');
    const container = document.getElementById('popup-container');
    
    if (!container) return;

    buttons.forEach(button => {
      button.addEventListener('click', async () => {
        const memberId = button.getAttribute('data-member-id');
        
        try {
          const { getTeamMembers } = await import('../data/team.js');
          const teamData = await getTeamMembers();
          const member = teamData[memberId];
          
          if (!member) return;
          
          container.innerHTML = `
            <div class="fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4" id="popup-backdrop">
              <div class="bg-white rounded-2xl max-w-2xl w-full max-h-[90vh] overflow-hidden shadow-2xl">
                
                <div class="sticky top-0 bg-gradient-to-r from-cyan-600 to-blue-600 text-white p-6 flex items-center justify-between">
                  <div class="flex items-center gap-4">
                    <img src="${member.image}" alt="${member.name}" class="w-16 h-16 rounded-full object-cover border-2 border-white" />
                    <div>
                      <h2 class="text-xl font-bold">${member.name}</h2>
                      <p class="text-cyan-100">${member.role}</p>
                    </div>
                  </div>
                  <button id="close-popup" class="text-white hover:rotate-90 transition-transform duration-300">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                  </button>
                </div>
                
                <div class="p-6 overflow-y-auto max-h-[calc(90vh-120px)]">
                  <p class="text-gray-700 leading-relaxed whitespace-pre-line">${member.bio}</p>
                  
                  ${member.email ? `
                    <div class="mt-6 pt-6 border-t border-gray-200">
                      <a href="mailto:${member.email}" class="inline-flex items-center gap-2 text-cyan-600 hover:text-cyan-700 font-medium">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                        </svg>
                        ${member.email}
                      </a>
                    </div>
                  ` : ''}
                </div>
              </div>
            </div>
          `;
          
          const closeBtn = document.getElementById('close-popup');
          const backdrop = document.getElementById('popup-backdrop');
          
          const closePopup = () => {
            container.innerHTML = '';
          };
          
          closeBtn?.addEventListener('click', closePopup);
          backdrop?.addEventListener('click', (e) => {
            if (e.target === backdrop) closePopup();
          });
          
          document.addEventListener('keydown', function escHandler(e) {
            if (e.key === 'Escape') {
              closePopup();
              document.removeEventListener('keydown', escHandler);
            }
          });
          
        } catch (error) {
          console.error('Error loading team member:', error);
        }
      });
    });
  });
</script>
